#include<iostream>
using namespace std;
int main()
{
    typedef int miles; // miles synonym for int
typedef char* c_string; // pointer to char
typedef void* gen_ptr; // generic pointer type
typedef point* point_ptr; // pointer to point

    return 0;
}